# Platform observability and provider policy milestone

- Added provider-specific protocol, monitoring, codec, resolution, frame-rate, bitrate, and keyframe policies.
- Enforced profile validation during profile creation and FFmpeg command generation.
- Added `/api/providers` policy discovery.
- Added `/api/system/metrics` with process, CPU, memory, disk, network, and event-loop metrics.
- Added filtered and paginated log queries plus CSV export.
- Expanded CI with Playwright and Docker build jobs.
