# Release Notes — 2026-07-08 Production Build Pass

## Summary

This pass focuses on making the uploaded SYCO23 Multicast Control archive buildable, auditable, and deployable from a clean checkout.

## Changes

- Added the missing `sql.js` runtime dependency required by the existing database abstraction.
- Updated Vite/Vitest toolchain to current non-vulnerable major versions available during this build pass.
- Added a repository-local ESLint configuration so `npm run lint` executes instead of failing due to missing config.
- Added production Docker packaging with Nginx static serving and `/healthz` healthcheck.
- Added Docker Compose runtime preset on port `8080`.
- Added PM2 preview deployment config for non-container hosts.
- Added GitHub Actions CI for install, audit, typecheck, lint, test, and build.
- Added README links to implementation, archival, and runbook documentation required by release-readiness tests.

## Verified Commands

```bash
npm run typecheck
npm run lint
npm test
npm run build
npm audit --audit-level=high
```

## Result

- TypeScript: passing
- ESLint: passing with warnings only
- Unit/integration tests: 169 passing
- Production build: passing
- npm audit high+: passing with 0 vulnerabilities

## Constraint

The requested GitHub repository `syco23-v4-multicast-control` was not visible to the connected GitHub app in this session, so this pass was applied to the uploaded archive and repackaged locally.
