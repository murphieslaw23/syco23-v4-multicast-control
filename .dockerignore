.git
# .github is deliberately NOT ignored: the Dockerfile runs `npm run release:check`
# in the build stage, and its config tests assert on the workflow definitions.
# Only the build stage sees these files; the runtime stage copies app/ and dist/.
node_modules
dist
coverage
playwright-report
test-results
data
*.sqlite
*.sqlite-*
*.log
.env
.env.*
!.env.example
docs/screenshots
SYCO23-v4.1-dark-ash-edition
